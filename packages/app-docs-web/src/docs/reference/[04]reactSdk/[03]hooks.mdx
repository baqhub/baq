export const metadata = {
  id: "622ecb15",
  title: "Hooks",
};

# Hooks

The React hooks in the BAQ React SDK make it possible to access and mutate the data in the [local store](f1e914a6), as well as in the user's server.

{props.pages}
