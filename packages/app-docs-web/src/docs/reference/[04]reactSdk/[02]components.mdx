export const metadata = {
  id: "a86fbc77",
  title: "Components",
};

# Components

The React components in the BAQ React SDK provide a shared context to the entire application that can then be consumed through [hooks](622ecb15).

{props.subPages}
