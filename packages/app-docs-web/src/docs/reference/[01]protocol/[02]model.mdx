export const metadata = {
  id: "163823f4",
  title: "Model",
};

# Model

The protocol is built on a small number of primitive data types that can be combined to build your application's data model.

{props.subPages}
