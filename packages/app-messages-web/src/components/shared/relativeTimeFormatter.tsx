//
// Props.
//

interface RelativeTimeFormatterProps {
  date: Date;
}

//
// Component.
//

// const rtf = new Intl.RelativeTimeFormat("en", {
//   numeric: "auto",
//   style: "short",
// });

// export const RelativeTimeFormatter: FC<RelativeTimeFormatterProps> = ({
//   date,
// }) => {
//   const text = useMemo(() => rtf.format(date), [date]);
// };
